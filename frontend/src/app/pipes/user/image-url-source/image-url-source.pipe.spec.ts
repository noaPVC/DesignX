import { ImageUrlSourcePipe } from './image-url-source.pipe';

describe('ImageUrlSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
